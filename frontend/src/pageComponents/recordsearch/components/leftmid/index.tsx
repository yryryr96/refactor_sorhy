'use client';

import { StyledLeftMidContainer } from './LeftMid.Styled';

const LeftMid = (props: any) => {
    const { userId } = props;

    return (
        <StyledLeftMidContainer>
            <div>good</div>
            <div>good</div>
        </StyledLeftMidContainer>
    );
};

export default LeftMid;
