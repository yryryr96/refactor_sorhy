'use client';

import { StyledLeftTopContainer } from './LeftTop.Styled';

const LeftTop = (props: any) => {
    const { userId } = props;

    return (
        <StyledLeftTopContainer>
            <div>good</div>
            <div>good</div>
        </StyledLeftTopContainer>
    );
};

export default LeftTop;
